#include <stdio.h>
#include <math.h>

int main() {

    int n ; 


   printf("affichage esst : %d", n);
   scanf("%d",&n); 



   int T[n] ,i; 
   int lasomme ; 
   for ( i ; i < n ; i ++ ){
    
   printf("affichage esst : %d",i + 1);
   scanf("%d",T[i]); 

 }
   lasomme = lasomme + T[i];

   printf("lafichage la somme estt : "); 

   for ( i ; i < n ; i ++ ){
   printf("affichage esst : %d", lasomme);
   

 }

